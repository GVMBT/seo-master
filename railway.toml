[deploy]
startCommand = "uv run --no-dev --frozen python scripts/seed_prompts.py --force && uv run --no-dev --frozen python -m bot"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
