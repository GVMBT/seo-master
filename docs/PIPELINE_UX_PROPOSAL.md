# Goal-Oriented Pipeline — UX Redesign Proposal

> **Статус:** Draft v1.7 (audit.md решения применены, готов к миграции в спеки)
> **Дата:** 2026-02-16
> **Автор:** Claude Opus 4.6
> **Цель:** Трансформация бота из "CMS с вкладками" в "воронку с результатом"
> **v1.7:** audit.md решения: §16.15 delete+send чеклист (не edit для сложных sub-flows),
>          §12.4+§4.3 Variant B inline handlers (не FSM delegation), §9.2 убран [Комбинировать],
>          §16.8 demo-статья → Phase D, §13 фазирование 5→4 (B+C объединены)
> **v1.6:** P0-fix: `city` → `company_city` (колонка уже существует), publication_logs.user_id прямой запрос.
>          P1-fix: Open Questions → Closed, миграция Quick Publish расширена (PRD/EDGE_CASES/ARCHITECTURE),
>          F34 миграция задокументирована
> **v1.5:** Закрытие gap'ов: лимит перегенераций (§4.1/§5.1), токеномика кросс-постинга (§5.2),
>          нумерация edge cases E49-E52 (§10.4), API_CONTRACTS cross-posting (§10.5),
>          publication_logs JOIN (§16.2), GOD_MODE (§16.24), Quick Publish миграция (§16.25),
>          F34 streaming убран (§16.26), Pinterest board уточнение (§5.1)
> **v1.4:** ButtonStyle карта (§18) — цветовая разметка всех кнопок pipeline (Bot API 9.4)
> **v1.3:** Sub-flow через отдельный Redis-ключ (§12.3-12.4), geography для автоподбора (§16.23),
>          Telegraph disclaimer, защита от выхода только шаги 4-7, running total в чеклисте,
>          решены все Open Questions (§16.18-16.22), pipeline_events analytics (Phase D)
> **v1.2:** Чеклист readiness на одном экране, убрана ловушка "Настроить подробнее",
>          объединены шаги 2+3 Social Pipeline, навигационные заголовки, возобновление pipeline,
>          кросс-постинг с ревью, контекстный CTA, batch mode, валидация WP, "только превью" path
> **v1.1:** Добавлены §16 (UX-решения), §7.5 (защита pipeline), обновлены Dashboard и Open Questions

---

## 1. Проблема

### 1.1. Текущее состояние

Бот спроектирован как набор инструментов: Dashboard показывает 5 равноправных кнопок, карточка проекта — ещё 7, карточка категории — ещё 10. Пользователь сам должен понять:
- В каком порядке нажимать кнопки
- Какие шаги обязательны, а какие нет
- Что будет, если пропустить цены или отзывы
- Зачем ему "Анализ сайта" перед публикацией

**Текущий путь к первой публикации — 34 экрана (минимум):**

```
 1. /start → Dashboard
 2. Dashboard → [Проекты]
 3. Project List → [Создать проект]
 4-7. ProjectCreate: name → company → specialization → website (4 ввода)
 8. → Project Card
 9. Project Card → [Подключения платформ]
10. Connection List → [WordPress]
11-13. ConnectWP: url → login → password (3 ввода)
14. → Connection List
15. Connection List → [К проекту]
16. Project Card → [Управление категориями]
17. Category List → [Создать категорию]
18. CategoryCreate: name (1 ввод)
19-20. → Category List → [Категория]
21. Category Card → [Ключевые фразы]
22. Keywords Main → [Подобрать]
23-26. KeywordGen: products → geography → quantity → confirm (4 ввода)
27-28. → Keyword Results → [К категории]
29. Category Card → [Опубликовать]
30. Publish Platform Choice → [WordPress]
31. Article Confirm → [Создать]
32-33. → generating → Article Preview
34. Article Preview → [Опубликовать]
```

С описанием (+3), ценами (+2), отзывами (+4), медиа (+2) — **до 45 экранов**.

Пользователь должен знать архитектуру (проект → подключение → категория → ключевики → публикация), чтобы дойти до результата. При этом он 6 раз вынужден возвращаться "назад" к родительскому экрану, чтобы перейти к следующему шагу.

### 1.2. Что хочет пользователь

Пользователь хочет одно из двух:
1. **"Напиши и опубликуй статью на мой сайт"**
2. **"Напиши и опубликуй пост в мой канал/группу"**

Всё остальное (проекты, категории, подключения, ключевики, описания, цены, отзывы) — это **средства**, а не цели. Пользователь не должен знать архитектуру — он должен получить результат.

### 1.3. Метафора

| Сейчас | Должно быть |
|--------|-------------|
| CMS-панель с вкладками | Воронка с результатом |
| "Вот 15 кнопок, разберись" | "Нажми одну кнопку, я всё проверю" |
| Пользователь — архитектор | Пользователь — заказчик |
| Бот — набор инструментов | Бот — ассистент с чеклистом |

---

## 2. Решение: Goal-Oriented Pipeline

### 2.1. Концепция

**Одна кнопка запускает весь пайплайн.** Бот сам проверяет, чего не хватает (readiness check), и предлагает дозаполнить по ходу. Что не хочешь — пропускаешь. Обязательное создаётся на месте. Результат — публикация.

### 2.2. Два режима сосуществуют

| Режим | Для кого | Вход | Что даёт |
|-------|----------|------|----------|
| **Pipeline** (новый) | Все пользователи | "Написать статью" / "Опубликовать пост" | Быстрый путь к результату, бот ведёт |
| **Toolbox** (текущий) | Power users | "Мои проекты" → карточка → инструменты | Полный контроль над каждой настройкой |

Toolbox не удаляется и не меняется. Pipeline — новый слой поверх существующих сущностей.

---

## 3. Новый главный экран (Dashboard)

### 3.1. Новый пользователь (0 проектов)

```
"Привет! Я помогу создать и опубликовать SEO-контент.
 Вам начислено 1500 токенов (~5 статей на сайт).
 
 Что хотите сделать?"

[Написать статью на сайт]        ← PRIMARY (синий)
[Опубликовать пост в соцсети]
───────────────────────
[Мои проекты]  [Профиль]
[Тарифы]       [Помощь]
```

### 3.2. Returning user (есть проекты и подключения)

```
"Баланс: 2 450 токенов
 Проектов: 3 | Расписаний: 5
 Хватит на: ~7 статей"

[Написать статью на сайт]        ← PRIMARY (синий)
[Опубликовать пост в соцсети]
───────────────────────
[Мои проекты]  [Профиль]
[Тарифы]       [Настройки]
[Помощь]
```

### 3.3. Reply-клавиатура (всегда видна внизу)

```
[Меню]  [Написать статью]
[АДМИНКА]*  (только для ADMIN_ID)
```

**Изменение:** "Быстрая публикация" → "Написать статью" (более понятная формулировка). Reply-кнопка запускает тот же pipeline, что и inline "Написать статью на сайт".

### 3.4. Что убирается с Dashboard

| Было | Стало | Почему |
|------|-------|--------|
| [Быстрая публикация] | [Написать статью на сайт] + [Опубликовать пост] | Разделение по цели, а не по скорости |
| [Настройки] (у новичка) | Убран с первого экрана | Новичку не нужны настройки, нужен результат |
| 5 равноправных кнопок | 2 CTA сверху + 4 навигации снизу | Визуальная иерархия: главное сверху |

---

## 4. Pipeline "Написать статью на сайт"

### 4.1. Полная схема

```
[Написать статью на сайт]
  │
  ├─ ШАГ 1: Выбор проекта
  │   ├── 0 проектов → "Для начала создадим проект — это 30 секунд."
  │   │    → встроенный ProjectCreate (4 вопроса) → автовозврат в pipeline
  │   ├── 1 проект → автовыбор, переход к шагу 2
  │   └── >1 проект → "Для какого проекта?"
  │        [Мебель Комфорт]  [Стоматология]  [Автосервис]  ...
  │        (пагинация по 8, как в текущем Quick Publish)
  │
  ├─ ШАГ 2: WordPress подключён?
  │   ├── Да, 1 подключение → автовыбор, переход к шагу 3
  │   ├── Да, >1 подключений → "На какой сайт?"
  │   │    [comfort-mebel.ru]  [blog.comfort.ru]
  │   └── Нет → "Для публикации нужен WordPress-сайт. Подключим?"
  │        [Подключить WordPress]  [Только превью (без публикации)]
  │        │
  │        ├── [Подключить] → встроенный ConnectWP (3 шага) → автовозврат
  │        └── [Только превью] → pipeline продолжается, 
  │             на шаге публикации будет только Telegraph-превью
  │
  ├─ ШАГ 3: Выбор категории (темы)
  │   ├── 0 категорий → "О чём будет статья? Назовите тему."
  │   │    → встроенный CategoryCreate (1 вопрос: название) → автовозврат
  │   ├── 1 категория → автовыбор, переход к шагу 4
  │   └── >1 категорий → "Какая тема?"
  │        [Кухонная мебель]  [Шкафы-купе]  [Диваны]  ...
  │
  ├─ ШАГ 4: Readiness check (только для незаполненного)
  │   │
  │   │  Бот проверяет чеклист и показывает ТОЛЬКО те шаги,
  │   │  где данных нет. Если всё заполнено — этот шаг пропускается целиком.
  │   │
  │   │  ФОРМАТ: все missing items показываются на ОДНОМ экране (чеклист),
  │   │  а не по одному вопросу. Пользователь видит полную картину сразу
  │   │  и выбирает, что заполнить. Это предотвращает ощущение "допроса".
  │   │
  │   │  Пример (при отсутствии ключевиков и описания):
  │   │  "Статья > Подготовка данных
  │   │
  │   │   Можно улучшить статью:
  │   │   ⬜ Ключевые фразы — SEO-оптимизация
  │   │   ⬜ Описание компании — точность контекста
  │   │   ✅ Цены — заполнено
  │   │   ✅ Фото — 4 AI-изображения
  │   │
  │   │   [Подобрать ключевики (100 ток.)]
  │   │   [Описание (20 ток.)]
  │   │   [Всё ОК — генерировать →]      ← SUCCESS (зелёный)
  │   │
  │   │   Ориентировочная стоимость: ~320 ток."
  │   │
  │   │  После заполнения каждого пункта — возврат на этот же чеклист
  │   │  с обновлёнными галочками и явным сообщением:
  │   │  "✓ Подобрано 12 кластеров (100 фраз). Что ещё?"
  │   │
  │   │  При выборе конкретного пункта:
  │   │
  │   ├── 4a. Ключевые фразы
  │   │    [Подобрать автоматически (100 фраз — 100 токенов)]
  │   │    [Настроить параметры]    → KeywordGen FSM (products → geography → quantity)
  │   │    [Загрузить свои]         → KeywordUpload FSM
  │   │    [Назад к чеклисту]
  │   │    │
  │   │    ├── [Подобрать автоматически] → использует company_name + specialization
  │   │    │    из проекта. Если у проекта нет `company_city` — один доп. вопрос:
  │   │    │    "В каком городе ваш бизнес? (для точных SEO-фраз)"
  │   │    │    [Москва] [СПб] [Ввести свой] [Вся Россия]
  │   │    │    Город сохраняется в проект, больше не спрашивается.
  │   │    │    → автовозврат к чеклисту
  │   │    ├── [Настроить] → встроенный KeywordGen → автовозврат к чеклисту
  │   │    └── [Загрузить] → встроенный KeywordUpload → автовозврат к чеклисту
  │   │
  │   ├── 4b. Описание компании/категории
  │   │    "Добавить описание? AI напишет точнее с контекстом о вашей компании."
  │   │    [Сгенерировать AI (20 токенов)]
  │   │    [Написать вручную]
  │   │    [Назад к чеклисту]
  │   │    │
  │   │    ├── [Сгенерировать] → AI генерирует → показ → [Сохранить] / [Перегенерировать]
  │   │    │    → "✓ Описание сохранено." → автовозврат к чеклисту
  │   │    └── [Вручную] → {Введите описание} → сохранить → автовозврат к чеклисту
  │   │
  │   ├── 4c. Цены
  │   │    "Добавить прайс-лист? В статье будут реальные цены ваших товаров."
  │   │    [Добавить текстом]
  │   │    [Загрузить Excel]
  │   │    [Назад к чеклисту]
  │   │
  │   ├── 4d. Фото
  │   │    Если у пользователя уже есть загруженные медиа (N штук):
  │   │    "Найдено N ваших фото. Использовать их + {4-N} AI?"
  │   │    [Да, комбинировать]  [Только мои (N)]  [Только AI (4)]
  │   │
  │   │    Если медиа нет:
  │   │    "В статье будет 4 изображения."
  │   │    [Загрузить свои фото]
  │   │    [AI сгенерирует (120 токенов)]
  │   │    [Настроить количество]
  │   │    [Назад к чеклисту]
  │   │
  │   └── 4e. Отзывы
  │        Показываются в чеклисте ТОЛЬКО при >5 публикациях.
  │        НЕ показывать новичкам — наименее влиятельный enricher.
  │        Доступен через toolbox (карточка категории) всегда.
  │
  ├─ ШАГ 5: Подтверждение
  │   "Статья > Подтверждение
  │
  │    Мебель Комфорт → comfort-mebel.ru
  │    Тема: Кухонная мебель
  │    Ключевики: 12 кластеров | Фото: 4 AI
  │
  │    Стоимость: ~320 ток. | Баланс: 2 450"
  │
  │   [Создать статью]        ← SUCCESS (зелёный)
  │   [Вернуться к чеклисту]  → шаг 4 (readiness check)
  │   [Отмена]
  │
  │   Примечание: кнопка "[Настроить подробнее]" убрана.
  │   Она вела в toolbox без возврата в pipeline — ловушка.
  │   Вместо неё: "[Вернуться к чеклисту]" для дозаполнения.
  │
  ├─ ШАГ 6: Генерация (~60 сек)
  │   "⏳ Собираю данные из Google..."
  │   "⏳ Анализирую конкурентов..."
  │   "⏳ Генерирую текст и изображения..."
  │   "⏳ Готовлю предпросмотр..."
  │   (параллельный pipeline: текст + изображения через asyncio.gather)
  │
  ├─ ШАГ 7: Превью + публикация
  │   "Статья > Превью
  │    📝 «Кухни на заказ в Москве: как выбрать и не переплатить»
  │    📊 2 150 слов, 4 картинки, 6 заголовков
  │    🔑 Кластер: Кухни на заказ (15 фраз)
  │    💰 320 токенов
  │    🔗 telegra.ph/Kuhni-na-zakaz-123
  │    ℹ️ Превью приблизительное. На сайте статья
  │       будет выглядеть в стиле вашей WP-темы."
  │
  │   Лимит перегенераций (FSM_SPEC §2.2):
  │   2 бесплатных перегенерации, далее платно (~320 ток.).
  │   Счётчик хранится в state.data["regen_count"].
  │   При regen_count >= 2: "[Перегенерировать (~320 ток.)]"
  │
  │   Вариант A (есть WP):
  │   [Опубликовать]          ← SUCCESS (зелёный)
  │   [Перегенерировать]      (или [Перегенерировать (~320 ток.)] если regen_count >= 2)
  │   [Отмена — вернуть токены] ← DANGER (красный)
  │
  │   Вариант B ("только превью", нет WP):
  │   [Подключить WordPress и опубликовать] ← SUCCESS (зелёный)
  │   [Скопировать HTML]
  │   [Перегенерировать]
  │   [Отмена — вернуть токены] ← DANGER (красный)
  │
  └─ ШАГ 8: Результат + next actions
      "✅ Опубликовано! 
       https://comfort-mebel.ru/kuhni-na-zakaz/
       Списано: 320 токенов | Остаток: 2 130"
      
      [Ещё статью]                    ← PRIMARY (синий) → pipeline с шага 3
      [Настроить автопубликацию]      → ScheduleSetup inline
      [Главное меню]
```

### 4.2. Поведение "Ещё статью"

После первой публикации pipeline запоминает выбранные проект и сайт. Повторный запуск **явно показывает контекст**:

```
[Ещё статью]
  → "Статья > Проект: Мебель Комфорт (comfort-mebel.ru)
     Какая тема?"
     [Кухонная мебель]  [Шкафы-купе]  [Диваны]
     [Другой проект]

  ├── Категорий >1 → список (как выше)
  ├── Категория 1 → автовыбор
  └── Всё заполнено → сразу подтверждение (шаг 5)
```

Кнопка `[Другой проект]` позволяет сменить контекст без выхода в главное меню.

**Для returning user с полностью настроенным проектом путь: 2-3 нажатия до генерации.**

### 4.3. Inline handlers (не FSM delegation)

Ключевое отличие от текущего UX: pipeline **сам содержит** states для каждого sub-flow (readiness_keywords, readiness_description и т.д.) — это НЕ вызов чужого FSM, а inline вопросы внутри PipelineFSM. Бот **автоматически возвращает в pipeline** после заполнения, а не выбрасывает на карточку или Dashboard.

Технически: PipelineFSM хранит все states, включая readiness sub-flows. Pipeline переиспользует Service Layer напрямую (KeywordService, ConnectionService, CategoryRepository), не делегируя в существующие FSM. Существующие FSM (KeywordGenerationFSM, ConnectWordPressFSM и др.) продолжают работать автономно из toolbox.

```
ArticlePipelineFSM (новый)
  │
  ├── state: select_project
  ├── state: create_project_*        # inline: 4 вопроса → ProjectRepository
  ├── state: check_wp / select_wp
  ├── state: connect_wp_*            # inline: 3 вопроса → ConnectionService
  ├── state: select_category
  ├── state: create_category         # inline: 1 вопрос → CategoryRepository
  ├── state: readiness               # чеклист — один экран
  ├── state: readiness_keywords_*    # inline: 3 вопроса → KeywordService
  ├── state: readiness_description   # inline: 1 confirm → AIOrchestrator
  ├── state: readiness_prices        # inline: text/excel → CategoryRepository
  ├── state: readiness_photos        # inline: upload/AI choice
  ├── state: confirmation
  ├── state: generating
  ├── state: preview
  └── state: published
```

---

## 5. Pipeline "Опубликовать пост в соцсети"

### 5.1. Полная схема

```
[Опубликовать пост в соцсети]
  │
  ├─ ШАГ 1: Выбор проекта
  │   (аналогично статье: 0→создать, 1→авто, >1→список)
  │
  ├─ ШАГ 2: Выбор подключения (платформа + канал в одном шаге)
  │   │
  │   │  Показываются конкретные подключения, а не платформы.
  │   │  Это устраняет лишний шаг выбора канала/группы.
  │   │
  │   ├── Есть подключения → "Пост > Куда публикуем?"
  │   │    [Telegram: @comfort_channel]
  │   │    [VK: Мебель Комфорт (группа)]
  │   │    [VK: Тест (группа)]
  │   │    [Pinterest: Мебельные идеи (доска: Интерьеры)]
  │   │    Примечание: Pinterest board выбирается при подключении
  │   │    (ConnectPinterestFSM) и сохраняется в credentials.
  │   │    Один board = одно подключение. Для разных досок — разные подключения.
  │   ├── 1 подключение → автовыбор
  │   └── 0 подключений → "Подключите соцсеть для публикации"
  │        [Подключить Telegram]
  │        [Подключить VK]
  │        [Подключить Pinterest]
  │        → встроенный Connect* FSM → автовозврат
  │
  ├─ ШАГ 3: Выбор категории (темы)
  │   (аналогично статье: 0→создать, 1→авто, >1→список)
  │
  ├─ ШАГ 4: Readiness check (сокращённый, формат чеклиста)
  │   │  Для соцпостов нужно меньше данных:
  │   │  - Ключевые фразы и описание — показываются в чеклисте (если не заполнены)
  │   │  - Цены, фото, отзывы — НЕ спрашивать для соцпостов
  │   │  - Если оба заполнены — шаг пропускается
  │   │
  │   │  "Пост > Подготовка данных
  │   │   ⬜ Ключевые фразы — релевантные хештеги
  │   │   ✅ Описание — заполнено
  │   │   [Подобрать ключевики (50 ток.)]
  │   │   [Всё ОК — генерировать →]"      ← SUCCESS (зелёный)
  │
  ├─ ШАГ 5: Подтверждение
  │   "Пост > Подтверждение
  │
  │    Telegram: @comfort_channel
  │    Тема: Кухонная мебель
  │    Стоимость: ~40 ток. | Баланс: 2 450"
  │
  │   [Создать пост] ← SUCCESS    [Вернуться к чеклисту]    [Отмена]
  │
  ├─ ШАГ 6: Генерация → ревью
  │   "Пост > Ревью
  │    📱 Пост для Telegram:
  │    ─────────────
  │    [текст поста в чате]
  │    ─────────────
  │    + 1 изображение"
  │
  │   [Опубликовать] ← SUCCESS    [Перегенерировать]*    [Отмена] ← DANGER
  │   * Лимит перегенераций: 2 бесплатных, далее платно (~40 ток.).
  │     Аналогично ArticlePublish (FSM_SPEC §2.2).
  │
  └─ ШАГ 7: Результат + next actions
      "✅ Пост опубликован в @comfort_channel!
       Списано: 40 токенов | Остаток: 2 410"

      [Адаптировать для VK (~10 ток.)]       ← показ. если есть VK
      [Адаптировать для Pinterest (~10 ток.)] ← показ. если есть Pinterest
      [Ещё пост]                                ← PRIMARY (синий)
      [Настроить автопубликацию]
      [Главное меню]

      Кнопки кросс-постинга показываются для ВСЕХ оставшихся
      подключённых платформ, а не только для одной.
```

### 5.2. Кросс-постинг

После публикации поста предлагается адаптация для других подключённых платформ. Адаптация текста (длина, хештеги, форматирование) без повторной генерации с нуля. Экономия: ~10 токенов вместо ~40. **Обязательный ревью** перед публикацией (§16.11).

**Токеномика кросс-постинга:**
- Стоимость: `ceil(adapted_word_count / 100) * 10` (~10 токенов для поста ~100 слов)
- Изображение: переиспользуется из оригинального поста (0 токенов)
- `token_expenses.operation_type`: `cross_post`
- В `API_CONTRACTS.md` MODEL_CHAINS добавить `task_type: "cross_post"` (DeepSeek, дешёвая трансформация)
- AI получает: оригинальный текст + правила целевой платформы (из `platform_rules/`)
- Результат: адаптированный текст, готовый к публикации

---

## 6. Сценарий: SEO-специалист с 10 клиентами

### 6.1. Первая настройка (15 мин на клиента)

```
[Написать статью на сайт]
  → "У вас нет проектов. Создадим?"
  → ProjectCreate: "Мебель Комфорт" / "Комфорт" / "Мебель" / comfort-mebel.ru
  → "Подключим WordPress?" → ConnectWP: URL / логин / пароль
  → "Создадим тему?" → CategoryCreate: "Кухонная мебель"
  → Readiness: ключевики → описание → цены
  → Генерация → Превью → Публикация

Повторить для следующего клиента: [Главное меню] → [Написать статью]
```

### 6.2. Ежедневная работа (3 нажатия)

```
[Написать статью на сайт]
  → "Для какого проекта?" → [Стоматология]
  → "Какая тема?" → [Имплантация]
  → Всё заполнено → подтверждение → [Создать статью]
```

**3 нажатия до генерации** вместо 12+ экранов.

### 6.3. Масштабируемость списков

| Элемент | Текущий Quick Publish | Новый Pipeline |
|---------|----------------------|----------------|
| 10 проектов × 3 категории × 2 платформы | 60 кнопок в одном списке | 10 → 2 → 3 = макс. 10 на экране |
| Пагинация | [Ещё ▼] при >8 | [Ещё ▼] при >8, но в каждом шаге отдельно |
| >100 комбинаций | Фоллбэк со сложной логикой | Не бывает — шаги разделены |

---

## 7. Обработка ошибок в Pipeline

### 7.1. Недостаточно токенов

```
Шаг 5 (подтверждение):
  "⚠️ Недостаточно токенов!
   Нужно: ~320 | Баланс: 50
   
   [Пополнить баланс] ← SUCCESS  → тарифы → после оплаты → автовозврат в pipeline
   [Отмена]"
```

Pipeline state сохраняется. После пополнения баланса бот предлагает продолжить:
"Баланс пополнен! Продолжить генерацию статьи?" [Продолжить] [Отмена]

### 7.2. WordPress не отвечает

```
Шаг 2 (проверка WP):
  "⚠️ Не удалось подключиться к comfort-mebel.ru
   Возможные причины: сайт недоступен, Application Password истёк.
   
   [Повторить попытку]   ← PRIMARY (синий)
   [Другой сайт]         → выбор из подключённых
   [Только превью]       → генерация без публикации
   [Отмена]"
```

### 7.3. Генерация не удалась

```
Шаг 6 (генерация):
  "⚠️ Ошибка генерации текста.
   Токены возвращены: 200
   
   [Повторить]           ← PRIMARY (синий) → заново с того же шага
   [Другая тема]         → шаг 3 (выбор категории)
   [Главное меню]"
```

### 7.4. FSM конфликт

Если пользователь находится в другом FSM (например, редактирует проект) и нажимает "Написать статью":
- Текущий FSM автоматически сбрасывается (существующее поведение, E29)
- Pipeline FSM стартует

### 7.5. Защита от случайного выхода

Если пользователь нажимает `[Меню]` (reply-кнопка) во время активного pipeline:

**Шаги 1-3** (выбор проекта/WP/категории): выход без подтверждения. Нечего терять — ничего ещё не заполнено и не потрачено.

**Шаги 4-7** (readiness → генерация → превью): подтверждение выхода:
```
"Вы в процессе создания статьи (подготовка данных).
 Прервать? Прогресс будет сохранён 24 часа."

[Продолжить pipeline]   ← PRIMARY (синий)
[Выйти в меню]          ← DANGER (красный)
```

Это предотвращает случайную потерю контекста на критических шагах, но не раздражает на ранних.

Аналогично для `/start` и любых команд, прерывающих pipeline.

### 7.6. Таймаут pipeline

Pipeline state в Redis с TTL 24h. Если пользователь ушёл на шаге 4c (цены) и вернулся через 2 часа:
- State сохранён, можно продолжить
- После 24h — state очищен, начать заново

---

## 8. Автопубликация в контексте Pipeline

### 8.1. Предложение расписания после первой публикации

```
Шаг 8 (после публикации):
  "✅ Статья опубликована!
   
   Хотите автоматические публикации по расписанию?
   Бот будет сам генерировать и публиковать статьи.
   
   [Каждый будний день в 10:00]     ← SUCCESS (зелёный) — quick preset
   [Пн, Ср, Пт в 10:00]            ← SUCCESS (зелёный) — quick preset
   [Настроить вручную]              → полный ScheduleSetup FSM
   [Позже]"
```

Quick presets создают расписание в 1 клик. Полная настройка — через существующий ScheduleSetup.

### 8.2. Автопубликация без участия юзера

Автопубликация по расписанию работает **без pipeline** — это существующий QStash cron механизм:
- QStash срабатывает по cron → webhook → генерация → публикация → уведомление
- Telegraph-превью не создаётся (нет юзера для ревью)
- При ошибке: возврат токенов + уведомление с причиной

Pipeline не меняет логику автопубликации. Он только упрощает **первоначальную настройку** расписания.

---

## 9. Фотографии: три источника изображений

### 9.1. Логика выбора

```
Приоритет:
1. Пользовательские фото (галерея медиа) — если загружены
2. AI-генерация (Gemini через OpenRouter) — дефолт
3. Без изображений — если пользователь выбрал 0
```

### 9.2. В pipeline (шаг 4d)

```
"В статье будет 4 изображения.

 Откуда взять?"
 [Загрузить свои фото]           → медиа-галерея, автовозврат
 [AI сгенерирует (120 токенов)]  → продолжаем с AI-генерацией
 [Настроить количество]          → выбор 0-10
```

После загрузки своих фото — контекстный вопрос вместо отдельной кнопки:
```
"Загружено N фото. Добавить AI-изображения?"
[Да, +{4-N} AI ({cost} ток.)]  [Нет, только мои N]
```
Комбинирование возникает **естественно** в потоке, без отдельной кнопки.

### 9.3. Стоимость

- AI-изображение: 30 токенов/шт
- Своё фото: 0 токенов (только хранение в Supabase Storage, бесплатно)
- Комбинация: например, 2 своих + 2 AI = 60 токенов вместо 120

### 9.4. Настройки AI-изображений

Базовые настройки (количество, формат) доступны в pipeline. Тонкие настройки (стили, камеры, ракурсы, негативные промпты) — в toolbox (карточка категории → "Настройки изображений").

---

## 10. Что меняется в существующих спеках

### 10.1. USER_FLOWS_AND_UI_MAP.md

| Секция | Изменение |
|--------|-----------|
| Часть 3: Dashboard | Новые CTA, иерархия кнопок |
| Быстрая публикация | Заменяется на Pipeline (два CTA: статья / пост) |
| Сценарий 1 | Обновить шаги 1-9 под pipeline flow |
| Новая секция | Pipeline "Написать статью" — полная схема |
| Новая секция | Pipeline "Опубликовать пост" — полная схема |
| Новая секция | Readiness check — логика проверки |

### 10.2. FSM_SPEC.md

| Изменение | Описание |
|-----------|----------|
| Новый FSM | `ArticlePipelineFSM` — 23 состояния (select_project → publishing + inline sub-flow states) |
| Новый FSM | `SocialPipelineFSM` — 10 состояний |
| Inline handlers | Pipeline НЕ вызывает существующие FSM; реализует inline handlers через Service Layer (§12.4 Variant B) |
| Существующие FSM | Без изменений — переиспользуются их Service Layer, но не сами FSM |

### 10.3. ARCHITECTURE.md

| Изменение | Описание |
|-----------|----------|
| Новый роутер | `routers/pipeline/` — article.py, social.py |
| Readiness service | `services/readiness.py` — проверка чеклиста |
| Dashboard update | `routers/start.py` — новые CTA |

### 10.4. EDGE_CASES.md

| Edge Case | Описание |
|-----------|----------|
| E49 (новый) | Pipeline прерван на mid-step (FSM timeout). Checkpoint в Redis жив 24ч — возобновление (§16.10) |
| E50 (новый) | Pipeline sub-flow fail (WP connect failed → вернуться в pipeline с ошибкой) |
| E51 (новый) | Pipeline + insufficient balance → пополнение → автовозврат из checkpoint (§16.19) |
| E52 (новый) | Pipeline кросс-постинг: адаптация failed → оригинальный пост уже опубликован, токены за cross-post возвращаются |

> **Нумерация:** E01-E48 заняты в EDGE_CASES.md (E43=outline fail, E44=critique rewrite,
> E45=NLP crash, E46=SimHash, E47=markdown parse, E48=anti-hallucination).
> Pipeline edge cases начинаются с E49.

### 10.5. API_CONTRACTS.md

| Изменение | Описание |
|-----------|----------|
| MODEL_CHAINS | Новый `task_type: "cross_post"` — DeepSeek V3.2, адаптация текста между платформами |
| token_expenses | Новый `operation_type: "cross_post"` — списание за кросс-постинг |
| PRD.md токеномика | Формула: `ceil(adapted_word_count / 100) * 10`, изображения = 0 (reuse) |

---

## 11. Что НЕ меняется

| Компонент | Почему не трогаем |
|-----------|------------------|
| Карточка проекта | Остаётся как toolbox для power users |
| Карточка категории | Полный набор инструментов доступен |
| Подключения платформ | Управление через карточку проекта |
| Планировщик | Детальная настройка через карточку проекта |
| Анализ сайта | Доступен через карточку проекта |
| Тарифы / Профиль | Без изменений |
| Помощь | Без изменений (можно добавить раздел про pipeline) |
| Админ-панель | Без изменений |
| Автопубликация (QStash) | Без изменений |
| API webhooks | Без изменений |
| Все существующие FSM | Работают как раньше + вызываются из pipeline как sub-flows |

---

## 12. Технические детали

### 12.1. Новые файлы

```
routers/pipeline/
  __init__.py           — регистрация роутеров
  article.py            — ArticlePipelineFSM (23 состояния)
  social.py             — SocialPipelineFSM (10 состояний)
  readiness.py          — inline readiness check handlers

services/
  readiness.py          — ReadinessService (проверка чеклиста проекта/категории)

keyboards/
  pipeline.py           — клавиатуры для pipeline (CTA, readiness, confirmation)
```

### 12.2. ReadinessService

```python
@dataclass
class ReadinessReport:
    has_keywords: bool
    keyword_count: int
    has_description: bool
    has_prices: bool
    has_media: bool
    media_count: int
    has_reviews: bool
    review_count: int
    estimated_cost: int          # токены
    user_balance: int
    is_sufficient_balance: bool
    
    @property
    def all_filled(self) -> bool:
        """Если всё заполнено — readiness check пропускается."""
        return self.has_keywords and self.has_description

    @property
    def missing_items(self) -> list[str]:
        """Только незаполненные пункты для показа в pipeline."""
        ...
```

### 12.3. Pipeline checkpoint (отдельный Redis-ключ)

Pipeline checkpoint хранится **отдельно от FSM state.data** в Redis-ключе `pipeline:{user_id}:state` с TTL 24h.

Причины:
- Aiogram хранит один FSM state per user. Sub-flow (KeywordGeneration и др.) пишут в корень `state.data`
- Если pipeline данные лежат в том же `state.data` — sub-flow может перезаписать их
- Отдельный ключ не требует рефакторинга существующих FSM (они работают без изменений)
- Pipeline checkpoint — это бизнес-логика, не FSM state

```python
# Redis key: pipeline:{user_id}:state, TTL 24h
{
    "pipeline_type": "article",        # или "social"
    "current_step": "readiness",       # текущий шаг pipeline
    "project_id": 5,
    "connection_id": 12,               # WP / TG / VK / Pinterest
    "category_id": 8,
    "skipped_keywords": False,
    "skipped_description": True,
    "skipped_prices": True,
    "skipped_photos": False,
    "photo_source": "ai",              # "user" | "ai" | "mixed"
    "image_count": 4,
    "estimated_cost": 320,
}
```

### 12.4. Sub-flow механизм (inline handlers)

Pipeline **не вызывает** существующие FSM (KeywordGenerationFSM, ConnectWordPressFSM и др.) как sub-flows. Вместо этого pipeline реализует **inline handlers**, переиспользуя Service Layer:

1. Pipeline сохраняет checkpoint в `pipeline:{user_id}:state` (Redis)
2. Pipeline переходит в свой state (напр. `readiness_keywords`) — это state **внутри** PipelineFSM
3. Inline handler задаёт нужные вопросы (products → geography → quantity)
4. По получении ответов вызывает сервис напрямую: `KeywordService.generate()`
5. По завершении обновляет checkpoint и возвращает в `readiness` state с обновлённым чеклистом

**Примеры inline handlers:**
- KeywordGeneration: 3 вопроса (products, geography, quantity) → `KeywordService.generate()` (~30-50 строк)
- ConnectWP: 3 вопроса (url, login, password) → `ConnectionService.create()` (~30-50 строк)
- CategoryCreate: 1 вопрос (название) → `CategoryRepository.create()` (~15 строк)
- Description: 1 подтверждение → `AIOrchestrator.generate("description")` (~20 строк)

**Преимущества Variant B (inline):**
- Полный контроль над pipeline flow (нет проблемы "как определить что KeywordGeneration закончился?")
- Существующие FSM вообще не трогаются — работают из toolbox как раньше
- Переиспользуется Service Layer, не дублируется логика
- Чеклист обновляется внутри pipeline, не нужен механизм `pipeline_return`

---

## 13. Приоритизация реализации

> Фазирование обновлено (audit.md #6): 5→4 фазы. Phase B и C объединены — inline handlers (§12.4) устраняют отдельный этап Sub-flow Return.

### Phase A: Core Pipeline + Dashboard

**Что:** ArticlePipelineFSM (шаги 1-3, 5-8 без readiness check) + новый Dashboard + удаление Quick Publish.

**Результат:** Пользователь нажимает "Написать статью" → выбирает проект → категорию → подтверждает → генерация → публикация.

**Объём:** ~4-5 новых файлов, изменения в start.py и keyboards, удаление quick.py.

### Phase B: Readiness Check + Inline Sub-flows

**Что:** ReadinessService + шаг 4 (ключевики, описание, цены, фото) + inline handlers для создания проекта / подключения WP / генерации ключевиков внутри pipeline (§12.4).

**Результат:** Pipeline проверяет чеклист, предлагает дозаполнить, создание сущностей происходит inline с автовозвратом в чеклист.

**Объём:** Самая объёмная фаза — inline handlers для 5 sub-flows + checkpoint + resume.

### Phase C: Social Pipeline + Cross-posting

**Что:** SocialPipelineFSM + кросс-постинг с обязательным ревью (§16.11).

### Phase D: Quick Presets + Batch + Analytics + Demo

**Что:** Quick presets для расписания (§8.1), batch mode (§16.14 Q3), pipeline_events analytics (§16.22), demo-статья (§16.8).

---

## 14. Метрики успеха

| Метрика | Текущее | Цель |
|---------|---------|------|
| Кликов до первой публикации (новичок) | **34** (мин.) / **45** (с данными) | 8-12 (с созданием проекта + ключевики) |
| Кликов до публикации (returning user) | 5-7 | 2-3 |
| "Навигация назад" (dead clicks) | 6 возвратов к родительским экранам | 0 (pipeline ведёт вперёд) |
| Drop-off rate на настройке | Высокий (гипотеза) | Ниже за счёт "Пропустить" |
| Время до первого результата | 15+ мин | 5-7 мин |
| Readiness пунктов для новичка | N/A (всё вручную) | 2 (ключевики + описание) |
| Повторное использование (returning) | Начать с Dashboard каждый раз | Pipeline помнит проект и настройки |

---

## 15. Closed Questions (решены в §16.1)

1. **Q1. Кросс-постинг:** [RESOLVED] Трансформация, ~10 токенов. См. §16.1, §5.2.
2. **Q2. "Только превью":** [RESOLVED] Telegraph + "Скопировать HTML". См. §16.1.
3. **Q3. Onboarding wizard:** [RESOLVED] Pipeline сам служит онбордингом. См. §16.1.
4. **Q4. Уведомление о расписании:** [RESOLVED] Да, summary показывается. См. §16.1.
5. **Q5. Pipeline vs Quick Publish:** [RESOLVED] Pipeline полностью заменяет. См. §16.1, §16.25.

---

## 16. UX-решения и дополнительные рекомендации

### 16.1. Ответы на Open Questions

**Q1. Кросс-постинг:** Трансформация, не повторная генерация. AI получает оригинальный текст + правила целевой платформы (длина, хештеги, emoji) и адаптирует. Стоимость: ~10 токенов (vs ~40 при генерации с нуля). UX: после публикации поста кнопка `[Тот же пост в VK]` показывается только если у проекта есть другие подключённые соцсети. Если подключена только одна платформа — кнопка не появляется.

**Q2. "Только превью":** Да, Telegraph + кнопка `[Скопировать HTML]`. Это демо-режим для оценки качества без WP. UX: после просмотра превью предложить `[Подключить WordPress и опубликовать]` — конверсия в подключение. Превью живёт 24ч (как обычные article_previews).

**Q3. Onboarding wizard:** Pipeline **сам является онбордингом**. Отдельный wizard не нужен. При первом запуске pipeline проводит через создание проекта → WP → категорию → ключевики → публикацию. Это эффективнее абстрактного "настройте ваш бот" — пользователь сразу видит результат (статью).

**Q4. Summary расписания:** Да, краткий summary обязателен:
```
"✅ Автопубликация настроена!
 Пн, Ср, Пт в 10:00
 ~960 токенов/неделю (~3 статьи)
 Баланс хватит на: ~7 статей

 [Изменить]  [ОК]"
```

**Q5. Pipeline vs Quick Publish:** Pipeline **полностью заменяет** Quick Publish. Quick Publish удаляется. Причины: (a) дублирование UX, (b) pipeline покрывает 100% сценариев Quick Publish, (c) pipeline делает это лучше (readiness check, sub-flow return). Кнопка `[Быстрая публикация]` на reply-клавиатуре заменяется на `[Написать статью]`.

### 16.2. Progressive Readiness (адаптивный чеклист)

Readiness check (шаг 4) адаптируется к опыту пользователя:

| Условие | Показываемые пункты | Почему |
|---------|---------------------|--------|
| **Первый запуск** (0 публикаций) | Только ключевики + описание | Не перегружать новичка. Цены, фото, отзывы — позже |
| **2-5 публикаций** | Ключевики + описание + цены + фото | Пользователь понял ценность, готов к деталям |
| **>5 публикаций** | Все пункты (если не заполнены) | Power user, знает что хочет |
| **Всё уже заполнено** | Шаг пропускается целиком | Returning user → 2-3 клика до генерации |

Логика: `publication_logs` имеет колонку `user_id` — запрос простой:
```sql
SELECT COUNT(*) FROM publication_logs WHERE user_id = ?
```
Порог настраивается через константы в `services/readiness.py`.

### 16.3. Первый запуск: упрощение ключевиков

На первом запуске pipeline шаг "Подобрать ключевые фразы" может быть пугающим (что вводить в "продукты"? "география"?). Предложение:

```
"Подберём SEO-фразы для лучшего ранжирования?

 [Подобрать автоматически (100 фраз — 100 токенов)]
 [Настроить параметры]
 [Пропустить]"
```

`[Подобрать автоматически]` использует `company_name` + `specialization` из проекта для заполнения полей products и geography. Пользователь не вводит ничего — 1 нажатие вместо 3 шагов.

`[Настроить параметры]` → стандартный KeywordGeneration FSM (products → geography → quantity).

### 16.4. Post-publish подсказки (hints)

После успешной публикации — ненавязчивые подсказки для улучшения:

```
Первая публикация:
  "💡 Совет: добавьте описание компании — статьи будут точнее отражать вашу специфику."
  [Добавить описание (20 токенов)]  [Позже]

Третья публикация (и нет цен):
  "💡 Совет: добавьте прайс-лист — в статьях появятся реальные цены ваших товаров."
  [Добавить цены]  [Позже]

Пятая публикация (и нет расписания):
  "💡 Совет: настройте автопубликацию — бот будет работать пока вы отдыхаете."
  [Настроить (1 клик)]  [Позже]
```

Подсказки показываются **один раз** (tracked через `users.onboarding_hints_shown` или Redis set). Не блокируют workflow. Можно отключить в настройках.

### 16.5. Dashboard: "Хватит на: ~7 статей" вместо "~1.3 нед"

Пользователь считает в статьях, а не в неделях. "~1.3 недели" требует знания расписания, количества подключений, средней длины статьи. "~7 статей" — интуитивно понятно.

Формула: `floor(balance / avg_article_cost)`, где `avg_article_cost = 320` (дефолт) или рассчитывается из последних 10 публикаций пользователя.

### 16.6. Автогенерация описания при пропуске

Если пользователь пропустил описание в pipeline (шаг 4b), AI всё равно использует доступный контекст:
- `company_name` + `specialization` из проекта
- `website_url` (если указан — Firecrawl scrape_branding для контекста)
- Названия категорий

Это не полноценное описание, но достаточно для первых статей. Pipeline не навязывает — просто использует что есть.

### 16.7. Крайний случай: >8 проектов (power user)

Для SEO-специалиста с 10+ клиентами список проектов может быть длинным. Предложение:

```
"Для какого проекта?"

[Мебель Комфорт]  ← последний использованный (выделен)
[Стоматология]    ← предпоследний
──────────────────
[Автосервис]  [Детский сад]
[Кофейня]     [Фитнес]
[Салон красоты]  [Юридическая]
[Ещё ▼]
```

Последний использованный проект — первым в списке. Это сокращает путь для returning power user.

### 16.8. Demo-статья для убеждения (Phase D)

> **Phase D** (не MVP). Pipeline сам является онбордингом (§16.1 Q3). Три CTA для новичка — перегрузка. Реализовать после Core Pipeline + Social Pipeline.

Идея для onboarding: кнопка `[Посмотреть пример статьи]` на Dashboard для новых пользователей. Показывает Telegraph-превью заранее сгенерированной статьи (статический пример, 0 токенов). Демонстрирует качество до того, как пользователь тратит токены.

```
Новый Dashboard:
  "Привет! Я помогу создать и опубликовать SEO-контент.
   Вам начислено 1500 токенов (~5 статей).

   [Написать статью на сайт]
   [Опубликовать пост в соцсети]
   [Посмотреть пример статьи →]     ← только для новичков
   ───────────────────────
   [Мои проекты]  [Профиль]
   [Тарифы]       [Помощь]"
```

Демо исчезает после первой публикации.

**Ограничение:** статическая демо-статья может не совпасть с нишей пользователя (стоматолог увидит статью про мебель). Варианты:
- Несколько демо-статей по популярным нишам (3-5 шт.), бот выбирает ближайшую
- Или одна универсальная ("Как малому бизнесу продвигаться в интернете")

### 16.9. Заголовки в сообщениях pipeline (навигационный контекст)

Каждое сообщение pipeline начинается с компактного заголовка, указывающего текущий этап:

```
"Статья > Шаг 1: проект"     — выбор проекта
"Статья > WordPress"          — проверка/подключение WP
"Статья > Шаг 3: тема"       — выбор категории
"Статья > Подготовка данных"  — readiness чеклист
"Статья > Подтверждение"      — финальное подтверждение
"Статья > Генерация..."       — процесс генерации
```

Зачем: если пользователь переключился на другой чат Telegram и вернулся — заголовок мгновенно напоминает, на каком этапе он остановился. Без этого сообщение "Какая тема?" непонятно вне контекста.

### 16.10. Возобновление незавершённого pipeline

Если пользователь прервал pipeline (ушёл из бота / нажал [Меню]) и возвращается позже:

```
"У вас есть незавершённая статья:
 Проект: Мебель Комфорт
 Остановились на: подготовка данных

 [Продолжить] ← SUCCESS    [Начать заново]    [Отменить] ← DANGER"
```

Показывается при входе в Dashboard, если в Redis есть активный pipeline state (TTL 24h).
Если пользователь нажимает `[Написать статью]` повторно — тоже показывается это сообщение (вместо начала нового pipeline).

### 16.11. Кросс-постинг: обязательный ревью

Адаптированный пост для другой платформы **всегда проходит ревью** перед публикацией. Причины:
- Разные платформы = разная аудитория и тон
- AI-адаптация может изменить смысл (сокращение текста, замена emoji)
- Пользователь должен контролировать финальный результат

```
[Тот же пост в VK]
  → "Пост адаптирован для VK:
     ─────────────
     [текст поста]
     ─────────────

     [Опубликовать в VK] ← SUCCESS    [Редактировать]    [Отмена]"
```

### 16.12. Контекстный порядок CTA на Dashboard

Порядок CTA-кнопок на Dashboard адаптируется к подключениям пользователя:

| Подключения | Порядок CTA | Логика |
|-------------|-------------|--------|
| Есть WP, нет соцсетей | `[Написать статью]` primary | Единственный путь к публикации |
| Есть соцсети, нет WP | `[Опубликовать пост]` primary | WP нет → статья невозможна без превью |
| Есть и WP, и соцсети | `[Написать статью]` primary (дефолт) | Статья — основной продукт |
| Нет подключений (новичок) | `[Написать статью]` primary | Pipeline сам предложит подключить |

Если у пользователя нет WP, но есть TG/VK — первая кнопка предлагает пост, а не статью. Это предотвращает "ложный первый шаг" для Персоны 3 (владелец магазина с Telegram-каналом без WordPress).

### 16.13. Progressive readiness: per-project vs глобальный порог

Пороги progressive readiness (§16.2) привязаны к **пользователю глобально**, а не per-project. Причина: SEO-специалист с 10 клиентами — опытный пользователь, даже если для нового проекта у него 0 публикаций. Знания об enrichers переносятся между проектами.

Исключение: если у проекта 0 категорий, все enrichers показываются (нет контекста для оптимизации чеклиста).

---

### 16.14. Решения по Open Questions из §17 (round 2)

**Q1. Pipeline + schedule: "Как в прошлый раз"**

При ручном запуске после настройки расписания — pipeline показывает быстрое подтверждение:
```
"Статья > Мебель Комфорт → comfort-mebel.ru
 Последняя тема: Кухонная мебель

 [Та же тема → генерировать]     ← SUCCESS (зелёный) — 1 клик до генерации
 [Другая тема]                    ← список категорий
 [Другой проект]"
```
Основано на `publication_logs ORDER BY created_at DESC LIMIT 1`. Не "запоминает навечно" — просто предлагает последний вариант.

**Q2. Количество изображений: per-category с override**

Дефолт берётся из настроек категории (toolbox). В pipeline показывается:
```
"В статье будет 4 изображения (настройка категории).
 [OK]  [Изменить для этой статьи]"
```
Power user задаёт 1 раз в toolbox и больше не видит этот шаг. Для исключений — override только на текущую генерацию.

**Q3. Express mode → Batch mode (Phase D)**

Express mode (1 клик = генерация) опасен: случайный клик = -320 токенов. Вместо этого — batch:
```
"Статья > Пакетная генерация
 Проект: Мебель Комфорт

 Выберите категории:
 [✅ Кухонная мебель]  [✅ Шкафы-купе]
 [⬜ Диваны]           [✅ Столы]

 Выбрано: 3 | Стоимость: ~960 ток.
 [Сгенерировать 3 статьи]  [Отмена]"
```
Результаты: список превью → `[Опубликовать все]` или по одной. Отложить на Phase D.

**Q4. Предложение расписания: после 1-й публикации**

Показывать сразу после 1-й — но мягко (hint, не блокер). Если нажал `[Позже]` — НЕ повторять на 2-й и 3-й. Напомнить на 5-й через post-publish hint (§16.4).

```
"💡 Автопубликация: бот сам будет генерировать и публиковать.
 [Пн, Ср, Пт в 10:00 (1 клик)]  [Позже]"
```

### 16.15. Readiness чеклист: обновление после sub-flow

Чеклист readiness (шаг 4) — это **одно сообщение** с текущими галочками. При возврате из sub-flow чеклист обновляется:

**Простые sub-flows** (1 сообщение → 1 ответ, напр. описание, цены): `editMessageText` того же сообщения чеклиста. Чеклист остаётся внизу чата — edit работает.

**Сложные sub-flows** (3-4 промежуточных сообщения, напр. KeywordGeneration: products → geography → quantity → результат): **deleteMessage старого чеклиста + отправить новое сообщение** внизу чата. Причина: после 3-4 сообщений sub-flow старый чеклист уехал вверх; `editMessageText` обновит его, но пользователь не увидит — он смотрит на низ чата.

Поток (сложный): чеклист (message_id=X) → "Подобрать ключевики" → inline sub-flow (3-4 сообщения) → `deleteMessage(X)` → **новое сообщение** чеклиста с обновлёнными галочками внизу чата.

### 16.16. Валидация WP на шаге выбора

При автовыборе WP-подключения (шаг 2, 1 подключение) — быстрая проверка доступности (HEAD-запрос, ~1 сек). Если сайт недоступен — сразу показать §7.2 (ошибка WP), а не ждать 60 сек генерации.

### 16.17. Pipeline checkpoint vs FSM timeout

Pipeline checkpoint хранится в отдельном Redis-ключе `pipeline:{user_id}:state` с TTL 24h (§12.3). Это **полностью независимо от FSM inactivity timeout** (30 мин для обычных FSM). Даже если FSM state сбросился по timeout — checkpoint жив, и при возврате пользователя pipeline восстанавливается из checkpoint (§16.10).

### 16.18. Множественные pipeline → подтверждение прерывания

Если пользователь начал ArticlePipeline для проекта A и нажимает "Опубликовать пост" (SocialPipeline для B):

```
"У вас активен pipeline:
 Статья для Мебель Комфорт (подготовка данных)

 [Прервать и начать новый] ← DANGER    [Вернуться к текущему] ← PRIMARY"
```

Checkpoint первого pipeline сохраняется 24h — если пользователь прервал, может вернуться позже (§16.10). Но одновременно два pipeline НЕ поддерживаются (один FSM state per user — ограничение Aiogram).

### 16.19. Возврат из YooKassa-оплаты в pipeline

Webhook handler проверяет наличие `pipeline:{user_id}:state` в Redis. Если есть — proactive message с подтверждением:

```
"Баланс пополнен! Продолжить создание статьи?
 Проект: Мебель Комфорт | Тема: Кухонная мебель

 [Продолжить] ← SUCCESS    [Нет]"
```

Нажатие `[Продолжить]` восстанавливает pipeline из checkpoint. Race condition (пользователь уже в другом FSM): при `[Продолжить]` текущий FSM сбрасывается (E29).

### 16.20. CTA для абсолютного новичка (0 подключений)

`[Написать статью на сайт]` остаётся primary. Причины:
- Pipeline **сам выяснит** отсутствие WP на шаге 2 и предложит подключить или "Только превью"
- Предварительный опрос "Что у вас есть?" — лишний шаг
- Статья — основной продукт и источник revenue (дороже постов)
- Пользователь, нажавший "не ту" кнопку, потеряет максимум 2 клика (проект → "нет WP")

### 16.21. BotFather: deep link + инструкция в чате

Подключение TG-канала требует создания бота через @BotFather (4 действия вне нашего бота). Минимизация friction:

```
"Для публикации в Telegram-канал нужен бот-публикатор.
 Создайте его за 30 секунд:

 1. Откройте @BotFather → [Открыть BotFather]     ← t.me/BotFather
 2. Отправьте /newbot
 3. Придумайте имя (например: Мой Контент Бот)
 4. Скопируйте токен и вставьте сюда ↓

 Затем добавьте бота админом в ваш канал."
```

Deep link `t.me/BotFather` открывает диалог. Полностью убрать friction невозможно (ограничение Telegram API), но хорошая инструкция + deep link сводят барьер к минимуму.

### 16.22. Pipeline analytics (Phase D)

Таблица `pipeline_events` для трекинга воронки:

```
pipeline_events(user_id, pipeline_id, event, step, metadata, created_at)

events: started, project_selected, wp_connected, wp_skipped,
        category_selected, readiness_completed, generation_started,
        published, dropped_at_step
```

Покажет где пользователи отваливаются — на readiness? на оплате? на превью? Без этих данных оптимизация вслепую. Реализовать в Phase D (после core pipeline).

### 16.23. Geography для автоподбора ключевиков

Автоподбор ключевиков (`[Подобрать автоматически]`) использует `company_name` + `specialization`, но для SEO критична география: "кухни на заказ" vs "кухни на заказ Москва" — разные запросы с разной конкурентностью.

Решение: при первом автоподборе, если у проекта нет `company_city`, задать один доп. вопрос:
```
"В каком городе ваш бизнес? (для точных SEO-фраз)
 [Москва]  [СПб]  [Ввести свой]  [Вся Россия]"
```
Город сохраняется в проект, больше не спрашивается.

> **Колонка уже существует:** `projects.company_city VARCHAR(255)` (ARCHITECTURE.md §3).
> Миграция не нужна. В коде: `project.company_city`.

### 16.24. GOD_MODE в pipeline

Для `ADMIN_ID` (GOD_MODE):
- Pipeline показывает стоимость каждого шага (как обычно)
- Токены **не списываются** (как в текущем toolbox)
- На экране подтверждения: `"Стоимость: ~320 ток. (GOD_MODE — бесплатно)"`
- На экране результата: `"Списано: 0 ток. (GOD_MODE) | Остаток: 2 450"`
- Кросс-постинг: аналогично, 0 токенов

Реализация: проверка `user_id == settings.ADMIN_ID` в `services/readiness.py` (estimated_cost) и `services/token.py` (charge_balance).

### 16.25. Quick Publish → Pipeline миграция

Pipeline **полностью заменяет** Quick Publish (§16.1, Q5). План удаления:

| Что | Действие |
|-----|----------|
| `keyboards/quick.py` | Удалить файл |
| `routers/publishing/quick.py` | Удалить файл |
| Reply-кнопка `[Быстрая публикация]` | Заменить на `[Написать статью]` в `keyboards/reply.py` |
| `quick:*` callback_data | Удалить обработчики. Новый формат: `pipeline:article:*`, `pipeline:social:*` |
| FSM_SPEC.md §2.1.1 | Обновить: Quick Publish → Pipeline reference |
| USER_FLOWS_AND_UI_MAP.md | Заменить секцию "Быстрая публикация" на pipeline описание |
| PRD.md F42 | Пометить "replaced by Pipeline (PIPELINE_UX_PROPOSAL.md)" |
| EDGE_CASES.md E28 | Обновить: "множественные WP через Quick Publish" → "множественные WP через Pipeline шаг 2" |
| ARCHITECTURE.md §2, §4.3 | Убрать `quick.py` из структуры, обновить примеры `quick:*` callback_data |
| CLAUDE.md | Обновить "Quick publish: 2-step flow" → "Pipeline: goal-oriented funnel" |

> **Важно:** миграция Quick Publish происходит в Phase A (Core Pipeline), не позже.
> До Phase A Quick Publish продолжает работать.

### 16.26. F34 Streaming → прогресс-сообщения

F34 (SSE streaming текста в чат) **убран из pipeline**. Вместо стриминга — прогресс-сообщения через `editMessageText` (шаг 6):

```
"⏳ Собираю данные из Google..."       (0-5 сек)
"⏳ Анализирую конкурентов..."         (5-15 сек)
"⏳ Генерирую текст и изображения..."  (15-50 сек)
"⏳ Готовлю предпросмотр..."           (50-60 сек)
```

Причина: стриминг в Telegram требует `sendMessageDraft` (Bot API 9.3) + forum topics в приватном чате — неоправданная сложность. Прогресс-сообщения дают достаточную обратную связь.

> **При миграции:** обновить PRD.md F34 — пометить "replaced by progress messages (§16.26)" или "deferred to v3 (sendMessageDraft)".
> Обновить CLAUDE.md: убрать "F34 streaming edge cases" из нерешённых вопросов.

---

## 17. Закрытые вопросы (round 2)

*Все вопросы (Q1-Q5 + 4 из §17 v1.2) решены в §16.1 и §16.14-16.23.*

Текущих открытых вопросов **нет**. Документ готов к превращению в рабочую спеку.

При реализации могут возникнуть новые вопросы — фиксировать здесь.

---

## 18. ButtonStyle — цветовая разметка кнопок (Bot API 9.4)

Bot API 9.4 добавил поле `style` в `InlineKeyboardButton` и `KeyboardButton`.
Aiogram 3.25 поддерживает через `aiogram.enums.ButtonStyle`:

```python
from aiogram.enums import ButtonStyle

# Доступные значения:
ButtonStyle.PRIMARY   # синий — главный CTA, точка входа
ButtonStyle.SUCCESS   # зелёный — подтверждение, "деньги → результат"
ButtonStyle.DANGER    # красный — деструктивное действие, потеря данных
# без style           # стандартный цвет — навигация, выбор, нейтральные действия
```

Также доступен `icon_custom_emoji_id: str` — кастомный эмодзи на кнопке (если бот имеет право использовать custom emoji в сообщении).

### 18.1. Стратегия применения

| Стиль | Цвет | Семантика | Правило |
|-------|------|-----------|---------|
| `PRIMARY` | синий | Главный CTA, точка входа в воронку | Максимум 1 синяя кнопка на экране |
| `SUCCESS` | зелёный | Подтверждение с последствиями (списание, публикация) | Только кнопки, после которых "назад" нельзя |
| `DANGER` | красный | Потеря работы, токенов, прогресса | Только деструктивные действия |
| *без стиля* | дефолт | Навигация, выбор, пропуск, enrichers | Всё остальное |

**Принцип:** цвет = уровень ответственности. Зелёный говорит "нажми и получи результат". Красный говорит "подумай, прежде чем нажимать". Синий говорит "начни здесь".

### 18.2. Полная карта по экранам

#### Dashboard (§3)

| Кнопка | Style | Почему |
|--------|-------|--------|
| `[Написать статью на сайт]` | `PRIMARY` | Главная точка входа в воронку |
| `[Опубликовать пост в соцсети]` | — | Вторичный CTA |
| `[Посмотреть пример статьи]` (новичок, §16.8) | — | Информационный, не action |
| `[Мои проекты]` `[Профиль]` `[Тарифы]` `[Помощь]` `[Настройки]` | — | Навигация |

#### Article Pipeline — Шаг 1: Выбор проекта (§4.1)

| Кнопка | Style | Почему |
|--------|-------|--------|
| Кнопки проектов `[Мебель Комфорт]` `[Стоматология]` ... | — | Нейтральный выбор |

#### Article Pipeline — Шаг 2: WordPress (§4.1)

| Кнопка | Style | Почему |
|--------|-------|--------|
| `[Подключить WordPress]` | `SUCCESS` | Продвигает pipeline вперёд, решает блокер |
| `[Только превью (без публикации)]` | — | Альтернативный путь |

#### Article Pipeline — Шаг 4: Readiness чеклист (§4.1)

| Кнопка | Style | Почему |
|--------|-------|--------|
| `[Подобрать ключевики (100 ток.)]` | — | Enricher, не финальное действие |
| `[Описание (20 ток.)]` | — | Enricher |
| `[Цены]` `[Фото]` | — | Enricher |
| **`[Всё ОК — генерировать →]`** | `SUCCESS` | Переход к подтверждению |
| `[Назад к чеклисту]` (из sub-flow) | — | Навигация |

Sub-flow кнопки внутри шага 4:

| Кнопка | Style | Почему |
|--------|-------|--------|
| `[Подобрать автоматически]` `[Настроить параметры]` `[Загрузить свои]` | — | Варианты выбора |
| `[Москва]` `[СПб]` `[Ввести свой]` `[Вся Россия]` (§16.23) | — | Выбор параметра |
| `[Сгенерировать AI]` `[Написать вручную]` | — | Варианты |
| `[Сохранить]` | `SUCCESS` | Фиксация результата |
| `[Перегенерировать]` | — | Повторное действие |

#### Article Pipeline — Шаг 5: Подтверждение (§4.1)

| Кнопка | Style | Почему |
|--------|-------|--------|
| **`[Создать статью]`** | `SUCCESS` | Списание токенов, старт генерации |
| `[Вернуться к чеклисту]` | — | Навигация назад |
| `[Отмена]` | — | Ничего не теряется |

#### Article Pipeline — Шаг 7: Превью (§4.1)

| Кнопка | Style | Почему |
|--------|-------|--------|
| **`[Опубликовать]`** | `SUCCESS` | Финальное действие — статья на сайте |
| `[Перегенерировать]` | — | Повторное действие |
| `[Отмена — вернуть токены]` | `DANGER` | Потеря сгенерированного контента |

Вариант B — "только превью" (нет WP):

| Кнопка | Style | Почему |
|--------|-------|--------|
| `[Подключить WordPress и опубликовать]` | `SUCCESS` | Решение блокера + публикация |
| `[Скопировать HTML]` | — | Вспомогательное действие |
| `[Перегенерировать]` | — | |
| `[Отмена — вернуть токены]` | `DANGER` | Потеря контента |

#### Article Pipeline — Шаг 8: Результат (§4.1)

| Кнопка | Style | Почему |
|--------|-------|--------|
| `[Ещё статью]` | `PRIMARY` | Повторный вход в pipeline |
| `[Настроить автопубликацию]` | — | Дополнительное действие |
| `[Главное меню]` | — | Навигация |

#### Social Pipeline — Шаг 2: Выбор подключения (§5.1)

| Кнопка | Style | Почему |
|--------|-------|--------|
| `[Telegram: @comfort_channel]` ... | — | Выбор |
| `[Подключить Telegram]` `[Подключить VK]` `[Подключить Pinterest]` | `SUCCESS` | Решение блокера |

#### Social Pipeline — Шаг 5: Подтверждение (§5.1)

| Кнопка | Style | Почему |
|--------|-------|--------|
| **`[Создать пост]`** | `SUCCESS` | Списание токенов |
| `[Вернуться к чеклисту]` | — | Навигация |
| `[Отмена]` | — | Ничего не теряется |

#### Social Pipeline — Шаг 6: Ревью (§5.1)

| Кнопка | Style | Почему |
|--------|-------|--------|
| **`[Опубликовать]`** | `SUCCESS` | Финальная публикация |
| `[Перегенерировать]` | — | |
| `[Отмена]` | `DANGER` | Потеря сгенерированного поста |

#### Social Pipeline — Шаг 7: Результат (§5.1)

| Кнопка | Style | Почему |
|--------|-------|--------|
| `[Адаптировать для VK (~10 ток.)]` | — | Дополнительное, не обязательное |
| `[Ещё пост]` | `PRIMARY` | Повторный вход |
| `[Настроить автопубликацию]` | — | |
| `[Главное меню]` | — | |

#### Ошибки (§7)

| Экран | Кнопка | Style | Почему |
|-------|--------|-------|--------|
| §7.1 Нехватка токенов | `[Пополнить баланс]` | `SUCCESS` | Решение проблемы |
| §7.1 | `[Отмена]` | — | |
| §7.1 Возврат после оплаты | `[Продолжить]` | `SUCCESS` | Восстановление pipeline |
| §7.2 WP недоступен | `[Повторить попытку]` | `PRIMARY` | Основное действие |
| §7.2 | `[Только превью]` `[Другой сайт]` `[Отмена]` | — | Альтернативы |
| §7.3 Генерация fail | `[Повторить]` | `PRIMARY` | Основное действие |
| §7.3 | `[Другая тема]` `[Главное меню]` | — | |

#### Защита от выхода (§7.5)

| Кнопка | Style | Почему |
|--------|-------|--------|
| **`[Продолжить pipeline]`** | `PRIMARY` | Предотвратить случайную потерю |
| `[Выйти в меню]` | `DANGER` | Потеря контекста pipeline |

#### Предложение расписания (§8.1)

| Кнопка | Style | Почему |
|--------|-------|--------|
| `[Каждый будний день в 10:00]` | `SUCCESS` | Quick preset — 1 клик до результата |
| `[Пн, Ср, Пт в 10:00]` | `SUCCESS` | Quick preset |
| `[Настроить вручную]` | — | Развёрнутый путь |
| `[Позже]` | — | |

#### Возобновление pipeline (§16.10)

| Кнопка | Style | Почему |
|--------|-------|--------|
| **`[Продолжить]`** | `SUCCESS` | Восстановление прогресса |
| `[Начать заново]` | — | Нейтральный выбор |
| `[Отменить]` | `DANGER` | Потеря сохранённого прогресса |

#### Кросс-постинг ревью (§16.11)

| Кнопка | Style | Почему |
|--------|-------|--------|
| **`[Опубликовать в VK]`** | `SUCCESS` | Финальная публикация |
| `[Редактировать]` | — | |
| `[Отмена]` | — | Пост уже опубликован в TG, потеря минимальна |

#### Quick confirm — "Как в прошлый раз" (§16.14)

| Кнопка | Style | Почему |
|--------|-------|--------|
| **`[Та же тема → генерировать]`** | `SUCCESS` | 1 клик до генерации |
| `[Другая тема]` | — | Навигация |
| `[Другой проект]` | — | Навигация |

#### Прерывание активного pipeline (§16.18)

| Кнопка | Style | Почему |
|--------|-------|--------|
| `[Прервать и начать новый]` | `DANGER` | Потеря текущего прогресса |
| **`[Вернуться к текущему]`** | `PRIMARY` | Безопасный выбор |

#### Возврат из YooKassa (§16.19)

| Кнопка | Style | Почему |
|--------|-------|--------|
| **`[Продолжить]`** | `SUCCESS` | Восстановление pipeline |
| `[Нет]` | — | |

### 18.3. Сводная статистика

```
SUCCESS (зелёный): 20 кнопок — все "деньги → результат" или "проблема → решение"
PRIMARY (синий):    8 кнопок — точки входа и "безопасный выбор"
DANGER  (красный):  6 кнопок — потеря контента / прогресса / токенов
Без стиля:        ~60 кнопок — навигация, выбор, пропуск, enrichers
```

### 18.4. Правила для разработчика

1. **Максимум 1 `PRIMARY` на экране.** Если есть `SUCCESS` — он важнее, `PRIMARY` не нужен
2. **`SUCCESS` только для необратимых действий:** списание токенов, публикация, сохранение. Не для навигации
3. **`DANGER` только при потере:** контента, прогресса, токенов. "Отмена" без потери — без стиля
4. **Enrichers всегда без стиля:** подбор ключевиков, описание, цены — это опции, не финальные действия
5. **`icon_custom_emoji_id`:** пока не используем — нет custom emoji sticker pack для бота. Добавить в Phase D при необходимости

### 18.5. Пример кода

```python
from aiogram.enums import ButtonStyle
from aiogram.types import InlineKeyboardButton, InlineKeyboardMarkup

# Dashboard — два CTA
dashboard_kb = InlineKeyboardMarkup(inline_keyboard=[
    [InlineKeyboardButton(
        text="Написать статью на сайт",
        callback_data="pipeline:article:start",
        style=ButtonStyle.PRIMARY,
    )],
    [InlineKeyboardButton(
        text="Опубликовать пост в соцсети",
        callback_data="pipeline:social:start",
    )],
    # ... navigation buttons without style
])

# Шаг 5 — подтверждение генерации
confirm_kb = InlineKeyboardMarkup(inline_keyboard=[
    [InlineKeyboardButton(
        text="Создать статью",
        callback_data="pipeline:article:generate",
        style=ButtonStyle.SUCCESS,
    )],
    [InlineKeyboardButton(
        text="Вернуться к чеклисту",
        callback_data="pipeline:article:readiness",
    )],
    [InlineKeyboardButton(
        text="Отмена",
        callback_data="pipeline:article:cancel",
    )],
])

# Шаг 7 — превью с деструктивной отменой
preview_kb = InlineKeyboardMarkup(inline_keyboard=[
    [InlineKeyboardButton(
        text="Опубликовать",
        callback_data="pipeline:article:publish",
        style=ButtonStyle.SUCCESS,
    )],
    [InlineKeyboardButton(
        text="Перегенерировать",
        callback_data="pipeline:article:regen",
    )],
    [InlineKeyboardButton(
        text="Отмена — вернуть токены",
        callback_data="pipeline:article:refund",
        style=ButtonStyle.DANGER,
    )],
])
```
